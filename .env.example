# =============================================================================
# FinAdvise MVP - Environment Variables Template
# =============================================================================
# 
# SECURITY NOTICE:
# - This file contains placeholder values only
# - NEVER commit actual secrets to version control
# - Copy this file to .env and set real values
# - Use strong, unique secrets for production environments
#
# =============================================================================

# -----------------------------------------------------------------------------
# CLAUDE CLI CONFIGURATION
# -----------------------------------------------------------------------------
# Claude session token for content generation
# Get this from your Claude account after authentication
CLAUDE_SESSION_TOKEN=your_claude_session_token_here

# -----------------------------------------------------------------------------
# GEMINI API CONFIGURATION  
# -----------------------------------------------------------------------------
# Google Gemini API key for image generation
# Format should start with "AIza"
# Get from: https://console.cloud.google.com/
GEMINI_API_KEY=your_gemini_api_key_here

# -----------------------------------------------------------------------------
# WHATSAPP BUSINESS API CONFIGURATION
# -----------------------------------------------------------------------------
# WhatsApp Business API credentials
# Get these from Meta Business Manager
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
WHATSAPP_BUSINESS_ACCOUNT_ID=your_business_account_id
WHATSAPP_ACCESS_TOKEN=your_whatsapp_access_token

# WhatsApp webhook configuration
# Use a strong, random string for verification
WHATSAPP_WEBHOOK_VERIFY_TOKEN=your_secure_webhook_verify_token_min_20_chars

# -----------------------------------------------------------------------------
# GOOGLE DRIVE & SHEETS CONFIGURATION
# -----------------------------------------------------------------------------
# Google Drive OAuth credentials
# Get these from Google Cloud Console
GOOGLE_DRIVE_CLIENT_ID=your_google_drive_client_id
GOOGLE_DRIVE_CLIENT_SECRET=your_google_drive_client_secret
GOOGLE_DRIVE_REFRESH_TOKEN=your_google_drive_refresh_token
GOOGLE_DRIVE_ROOT_FOLDER_ID=your_drive_folder_id

# Google Sheets configuration
GOOGLE_SHEETS_ID=your_google_sheets_id
GOOGLE_SHEETS_CREDENTIALS=/path/to/your/google-credentials.json

# -----------------------------------------------------------------------------
# EMAIL CONFIGURATION (SMTP)
# -----------------------------------------------------------------------------
# SMTP settings for email notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@domain.com
SMTP_PASS=your_app_specific_password

# -----------------------------------------------------------------------------
# WEBHOOK SECURITY CONFIGURATION
# -----------------------------------------------------------------------------
# CRITICAL: Use strong, unique secrets (minimum 20 characters)
# Generate with: openssl rand -base64 32
WEBHOOK_SECRET=your_super_secure_webhook_secret_min_20_characters

# Webhook URLs (update with your actual server URLs)
WEBHOOK_LOCAL_URL=http://localhost:5001/trigger
WEBHOOK_PRODUCTION_URL=https://your-domain.com:5001/trigger
WEBHOOK_PORT=5001

# -----------------------------------------------------------------------------
# ADMIN CONFIGURATION
# -----------------------------------------------------------------------------
# Admin WhatsApp numbers (comma-separated, international format)
# Example: +1234567890,+0987654321
ADMIN_WHATSAPP_NUMBERS=+your_admin_numbers_here

# -----------------------------------------------------------------------------
# APPLICATION CONFIGURATION
# -----------------------------------------------------------------------------
# Environment setting
NODE_ENV=production

# Logging level (error, warn, info, debug)
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# SECURITY RECOMMENDATIONS
# -----------------------------------------------------------------------------
#
# 1. WEBHOOK_SECRET: Use a cryptographically secure random string:
#    - Minimum 20 characters
#    - Include letters, numbers, and symbols
#    - Generate with: openssl rand -base64 32
#
# 2. API Keys: 
#    - Never share or commit to version control
#    - Rotate regularly (every 90 days recommended)
#    - Use different keys for development and production
#
# 3. Access Tokens:
#    - Set proper expiration times
#    - Monitor for unusual activity
#    - Revoke immediately if compromised
#
# 4. File Permissions:
#    - Set .env file permissions to 600 (owner read/write only)
#    - Use: chmod 600 .env
#
# 5. Production Deployment:
#    - Use environment variable injection rather than .env files
#    - Consider using secrets management services (AWS Secrets Manager, etc.)
#    - Never store secrets in Docker images
#
# =============================================================================