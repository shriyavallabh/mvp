<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agent Monitor - FinAdvise Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <script src="/socket.io/socket.io.js"></script>
    <link rel="stylesheet" href="/css/dashboard.css">
    <style>
        .agent-node {
            background: white;
            border: 2px solid #dee2e6;
            border-radius: 8px;
            padding: 10px 15px;
            margin: 5px;
            transition: all 0.3s;
        }
        .agent-node.processing {
            border-color: #0d6efd;
            background: #e7f1ff;
            animation: pulse 1.5s infinite;
        }
        .agent-node.completed {
            border-color: #28a745;
            background: #d4edda;
        }
        .agent-node.error {
            border-color: #dc3545;
            background: #f8d7da;
        }
        @keyframes pulse {
            0% { box-shadow: 0 0 0 0 rgba(13, 110, 253, 0.7); }
            70% { box-shadow: 0 0 0 10px rgba(13, 110, 253, 0); }
            100% { box-shadow: 0 0 0 0 rgba(13, 110, 253, 0); }
        }
        .agent-hierarchy {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .agent-children {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            margin-top: 20px;
            position: relative;
        }
        .agent-children::before {
            content: '';
            position: absolute;
            top: -10px;
            left: 50%;
            width: 2px;
            height: 10px;
            background: #dee2e6;
        }
        .timeline-item {
            border-left: 3px solid #dee2e6;
            padding-left: 20px;
            margin-left: 10px;
            position: relative;
            padding-bottom: 20px;
        }
        .timeline-item::before {
            content: '';
            position: absolute;
            left: -7px;
            top: 5px;
            width: 11px;
            height: 11px;
            border-radius: 50%;
            background: #6c757d;
        }
        .timeline-item.processing::before {
            background: #0d6efd;
        }
        .timeline-item.completed::before {
            background: #28a745;
        }
        .timeline-item.error::before {
            background: #dc3545;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    
    <div class="container-fluid">
        <div class="row">
            <%- include('partials/sidebar') %>
            
            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
                <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                    <h1 class="h2">Live Agent Execution Viewer</h1>
                    <div class="btn-toolbar mb-2 mb-md-0">
                        <button type="button" class="btn btn-sm btn-primary" onclick="simulateExecution()">
                            <i class="bi bi-play-fill"></i> Simulate Execution
                        </button>
                        <button type="button" class="btn btn-sm btn-outline-secondary ms-2" onclick="clearTimeline()">
                            <i class="bi bi-trash"></i> Clear Timeline
                        </button>
                    </div>
                </div>

                <div class="row mb-4">
                    <div class="col-md-8">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="mb-0">Agent Hierarchy</h5>
                            </div>
                            <div class="card-body" style="min-height: 400px;">
                                <div id="agent-hierarchy" class="agent-hierarchy">
                                    <div class="text-center text-muted">Loading agent hierarchy...</div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-md-4">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="mb-0">Agent Status Summary</h5>
                            </div>
                            <div class="card-body">
                                <div class="mb-3">
                                    <label class="text-muted small">Total Agents</label>
                                    <h4 id="total-agents">0</h4>
                                </div>
                                <div class="row">
                                    <div class="col-4">
                                        <label class="text-muted small">Idle</label>
                                        <h5 class="text-secondary" id="idle-agents">0</h5>
                                    </div>
                                    <div class="col-4">
                                        <label class="text-muted small">Running</label>
                                        <h5 class="text-primary" id="running-agents">0</h5>
                                    </div>
                                    <div class="col-4">
                                        <label class="text-muted small">Completed</label>
                                        <h5 class="text-success" id="completed-agents">0</h5>
                                    </div>
                                </div>
                                <hr>
                                <div>
                                    <label class="text-muted small">Manual Triggers</label>
                                    <div class="d-grid gap-2">
                                        <button class="btn btn-sm btn-outline-primary" onclick="triggerAgent('content-orchestrator')">
                                            <i class="bi bi-play"></i> Content Orchestrator
                                        </button>
                                        <button class="btn btn-sm btn-outline-primary" onclick="triggerAgent('approval-guardian')">
                                            <i class="bi bi-play"></i> Approval Guardian
                                        </button>
                                        <button class="btn btn-sm btn-outline-primary" onclick="triggerAgent('distribution-manager')">
                                            <i class="bi bi-play"></i> Distribution Manager
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="mb-0">Execution Timeline</h5>
                            </div>
                            <div class="card-body" style="max-height: 400px; overflow-y: auto;">
                                <div id="execution-timeline">
                                    <div class="text-center text-muted">No executions yet</div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-md-6">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="mb-0">Live Agent Logs</h5>
                            </div>
                            <div class="card-body">
                                <div class="mb-2">
                                    <select class="form-select form-select-sm" id="agent-log-selector">
                                        <option value="">Select an agent...</option>
                                        <option value="content-orchestrator">Content Orchestrator</option>
                                        <option value="content-strategist">Content Strategist</option>
                                        <option value="content-generator">Content Generator</option>
                                        <option value="fatigue-checker">Fatigue Checker</option>
                                        <option value="compliance-validator">Compliance Validator</option>
                                        <option value="distribution-manager">Distribution Manager</option>
                                    </select>
                                </div>
                                <div id="agent-logs" style="max-height: 300px; overflow-y: auto; font-family: monospace; font-size: 12px; background: #f8f9fa; padding: 10px; border-radius: 4px;">
                                    <div class="text-center text-muted">Select an agent to view logs</div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/agents.js"></script>
</body>
</html>